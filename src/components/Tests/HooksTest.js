import React, { useState } from 'react';

const HooksTest = () => {
//     const [showDiv, setShowDiv] = useState(false)
//     const [formObj, setFormObj] = useState({})

    return (
        <div>
            <button>Click to show big div!</button>
        </div>
    );
};

export default HooksTest;
